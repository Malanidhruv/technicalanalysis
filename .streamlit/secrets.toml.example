# Streamlit Cloud: App settings → Secrets → paste this block with your values.
# AliceBlue NEW auth (appCode + apiSecret). Do NOT use the old user_id/api_key format.

[aliceblue]
app_key = "YOUR_APP_CODE"
api_secret = "YOUR_API_SECRET"
