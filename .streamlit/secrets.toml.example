# Copy to secrets.toml and fill in your AliceBlue app credentials.
# On Streamlit Cloud: App Settings → Secrets (paste this block).

[aliceblue]
app_key = "YOUR_APP_KEY"
api_secret = "YOUR_API_SECRET"
